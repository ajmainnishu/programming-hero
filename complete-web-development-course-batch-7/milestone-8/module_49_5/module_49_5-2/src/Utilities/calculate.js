// const add = (first, second) => {
//     return first + second;
// }

// export default add;




const add = (first, second) => {
    return first + second;
}
const multiply = (first, second) => {
    return first * second;
}

export {add, multiply};